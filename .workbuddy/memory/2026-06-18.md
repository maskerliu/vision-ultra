# 人脸采集录入与 LanceDB 功能完善

## 2026-06-18
- 分析并修复了人脸采集录入与 LanceDB 相关功能的 5 个核心文件
- 关键修复：
  1. `facerec.service.ts` 中被注释掉的数据库插入逻辑恢复（人脸注册终于会写入数据库）
  2. `ProcessorManager.ts` 中 `faceCapture` 的过早 `return` 移除，采集逻辑真正生效
  3. `facerec.repo.ts` 升级 LanceDB 0.30 API：`search()` → `query().nearestTo()`，修正 `_rowid` 大小写，批量删除，索引自动检查创建
  4. `facerec.api.ts` 修复 `recognize` 的 MIME 类型和向量转换
  5. `FaceDbMgr.vue` 修复删除单个特征时 `selectedEigenId` 未赋值，增加删除后列表刷新和错误提示
- 详细修改报告见 `.workbuddy/artifacts/face-rec-lancedb-improvements.md`
